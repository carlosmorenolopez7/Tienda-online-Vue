<script setup>
defineProps(['producto','carrito'])

function eliminarProducto(producto,carrito){
      carrito.splice(carrito.indexOf(producto),1)
}
</script>

<template>
<div class="producto-carrito">
      <input type="hidden" class="id-producto-carrito" :value="producto.id">
      <div class="contenedor-imagen-producto-carrito">
            <img class="imagen-producto-carrito" :src="producto.imagen" :alt="producto.nombre">
      </div>
      <div class="contenedor-informacion-producto-carrito">
            <h1 class="nombre-producto-carrito titulo">{{producto.nombre}}</h1>
            <div class="contenedor-imagen-eliminar hover" @click="eliminarProducto(producto,carrito)">
                  <img class="imagen-eliminar" src="https://upload.wikimedia.org/wikipedia/commons/thumb/5/5f/Red_X.svg/1024px-Red_X.svg.png" alt="eliminar producto">
            </div>
            <span class="cantidad-producto-carrito-menos">-</span>
            <span class="cantidad-producto-carrito-numero"></span>
            <span class="cantidad-producto-carrito-mas">+</span>
            <span class="precio-producto-carrito">{{producto.precio}} €</span>
      </div>
</div>
</template>